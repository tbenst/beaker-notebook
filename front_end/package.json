{
  "scripts": {
    "bower": "bower install",
    "watch": "roots watch",
    "compile": "roots compile --compress",
    "dev-compile": "roots compile --no-compress",
    "coverage": "NODE_ENV=test npm run-script dev-compile && node coverage-server"
  },
  "devDependencies": {
    "bower": "1.3.12",
    "roots": "2.1.4",
    "istanbul": "0.3.0",
    "istanbul-middleware": "^0.1.1",
    "express": "^4.1.1"
  }
}
