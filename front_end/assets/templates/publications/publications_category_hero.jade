.category-hero(ng-if='publications.category')
  .category
    h2 {{publications.category.name}}
    .category-information
      p.description {{publications.category.description}}
      .notebooks
        .title Notebooks
        .quantity {{publications.quantity}}
  .cl
