{{project.name}}
ul
  li(ng-show="notebooks.length === 0") no notebooks yet.
  li(ng-repeat="notebook in notebooks")
    a.notebook(ui-sref-active="active", ui-sref="projects.item.notebook({id: project.id, name: notebook.name})") {{notebook.name | limitTo:22}}
