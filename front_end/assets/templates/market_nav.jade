treecontrol.tree-classic(
   tree-model="treeData",
   options="treeOptions"
   on-selection="onTreeSelection(node)")

  item: {{node.category}} - ({{node.count}})

br
.label-pair
  label
    b Related Tags
  br
  dl
    dt(ng-repeat="tag in marketPlace.relatedTags")
      a(ng-click="searchByTag(tag)" href='') {{tag.name}}
