h1 your projects
input.search(ng-model="projectSearch", placeholder="search")
ul
  li(ng-show="projects.list.length === 0") no projects yet.
  li(ng-repeat="project in projects.list")
    a.project(ui-sref-active="active", ui-sref="projects.item({id: project.id})") {{project.name | limitTo:22}}...
