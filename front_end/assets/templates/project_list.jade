input.search-full.project-search(ng-model="projects.search", placeholder="search")
h3.sidebar-box-header Projects
.sidebar-box
  ul.project-results
    li(ng-show="projects.list.length === 0") no projects yet.
    li(ng-repeat="project in projects.list")
      a.project(ui-sref-active="active", ui-sref="projects.items.item({id: project.id})") {{project.name | limitTo:30}}

  br
  .btn.create-project(ng-click="createProject()") New project
