{
  "name": "bunsen-features",
  "version": "0.0.0",
  "description": "Specifying big data for fun and profit by example",
  "main": "dill",
  "scripts": {
    "start": "NODE_ENV=test dill features/ --require util/ --require step_definitions/ --require widgets/ -f pretty",
    "test": "NODE_ENV=test node-debug --web-port 8081 --debug-port 5859 dill features/ --require util/ --require step_definitions/ --require widgets/"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/twosigma/bunsen.git"
  },
  "author": "",
  "bugs": {
    "url": "https://github.com/twosigma/bunsen/issues"
  },
  "homepage": "https://github.com/twosigma/bunsen",
  "dependencies": {
    "dill": "0.1.0",
    "lodash": "2.4.1",
    "request": "2.33.0",
    "bluebird": "1.2.1",
    "moment": "^2.6.0"
  },
  "devDependencies": {
    "node-inspector": "0.7.0"
  }
}
